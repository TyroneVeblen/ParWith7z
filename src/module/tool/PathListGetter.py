import os
from copy import deepcopy


class PathListGetter:
    def __init__(self, path, folder_only, path_depth):
        self.path = path
        self.folder_only = folder_only
        self.path_depth = path_depth



